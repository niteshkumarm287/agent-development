// content.js is intentionally minimal.
// popup.js uses chrome.scripting.executeScript to
// directly call document.body.innerText in the page context.
// No listener needed here unless you want right-click menus later.